# =============================================================================
# Core — Web framework & runtime
# =============================================================================
fastapi==0.111.0 # FastAPI web framework
uvicorn[standard]==0.30.1 # ASGI server for running FastAPI applications
python-dotenv==1.0.1 # Load environment variables from .env file
pydantic==2.7.1 # Data validation and settings management
pydantic-settings==2.3.0 # Settings management for Pydantic

# =============================================================================
# Database — SQLAlchemy async, SQL Server ODBC, migrations
# =============================================================================
sqlalchemy[asyncio]==2.0.30 # SQLAlchemy with async support
aioodbc==0.5.0 # Async ODBC driver for SQL Server
pyodbc==5.1.0 # ODBC driver for SQL Server
alembic==1.13.1 # Database migrations tool

# =============================================================================
# Dev / Test — HTTP client, pytest suite
# =============================================================================
httpx==0.27.0 # Async HTTP client for testing FastAPI endpoints
pytest==8.2.1 # Testing framework
pytest-asyncio==0.23.7 # Async support for pytest
pytest-cov==5.0.0 # Code coverage reporting for pytest
anyio==4.4.0 # Async I/O library for testing and async operations
